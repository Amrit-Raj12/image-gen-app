const PRIMARY = '#000000';
const SECONDARY = '#601BC4';
const CONTRAST = '#FFFFFF';
const DARK = '#354A21';
const INACTIVE_CONTRAST = 'rgba(255, 255, 255, 0.5)';
const ERROR = '#FF5B14';
const SUCCESS = '#3BEF1E';
const OVERLAY = 'rgba(45, 35, 35, 0.5)';

const colors = {
  PRIMARY,
  SECONDARY,
  CONTRAST,
  DARK,
  INACTIVE_CONTRAST,
  ERROR,
  SUCCESS,
  OVERLAY,
};

export default colors;
